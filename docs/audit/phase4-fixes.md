# Phase 4 — Fix List

All fixes to be applied in Phase 4, ordered by severity then area.

## Fix Table

| Fix ID | Area | File:Line | Description | Severity | Source Phase |
|--------|------|-----------|-------------|----------|--------------|
| phase4-fix-1 | instructor-dashboard | `edulearn.client/src/components/Dashboard/InstructorDashboard.jsx:103` | Stat cards are not clickable — no `onClick` handler on `.rd-stat-card` divs. Instructors cannot drill into section, course, or assessment detail from their dashboard. | Medium | Pre-planned |
| phase4-fix-2 | content-uri | `edulearn.client/src/pages/contents/ContentDetailPage.jsx:180` | Content URI is shown in a read-only `<input>` with a copy button, but the URI is only rendered as a clickable link when `isLinkable` is true (`http`/`https` prefix detected). URIs without a recognised scheme (e.g. `s3://`, `ftp://`, or relative paths) show no link. The copy-only experience is acceptable but the "Open Resource" button should not be gated on protocol alone. | Medium | Pre-planned |
| phase4-fix-3 | invoices | `edulearn.client/src/pages/finance/InvoicesPage.jsx:16` | Student role sees the full Student ID search bar and can query any student's invoices. Auto-load own invoices using `userId` from `authService.getCurrentUser()` for the Student role instead; hide the search bar for students. Backend fix also required: `InvoicesController.cs:246,269` — bare `[Authorize]` must be tightened to `FinancePolicy` with student-ownership exception. | Medium | Pre-planned |
| phase4-fix-4 | enrollment | `edulearn.client/src/pages/enrollment/EnrollmentPage.jsx:150` | Student role sees a full program dropdown and course dropdown showing all programs and all courses institution-wide. The page should auto-scope to the student's own enrolled program (from their student record) and only show sections of their program's courses. | Medium | Pre-planned |
| phase4-fix-5 | kpis | `EduLearn.API/Repositories/Implementations/ReportRepository.cs:101` | `RecalculateAndSaveAsync` matches KPI names via a raw string `switch` (`"Active Student Count"`, `"Section Fill Rate"`, etc.). These literal strings must match the seeded KPI names exactly; a name mismatch silently leaves a KPI unchanged. Extract KPI name constants to a static class and use them in both the seed data and the switch to prevent silent drift. | Medium | Pre-planned |
| phase4-fix-6 | rbac-backend | `EduLearn.API/Controllers/PaymentsController.cs:106` | `GET /api/payments/invoice/{invoiceId}` uses bare `[Authorize]` — all 7 roles can read payment amounts. Restrict to `[Authorize(Policy = "FinancePolicy")]` with a student-ownership bypass for own invoices. | High | Phase 2 |
| phase4-fix-7 | rbac-backend | `EduLearn.API/Controllers/InvoicesController.cs:246,269` | `GET /api/invoices/student/{studentId}` and `GET /api/invoices/{id}` use bare `[Authorize]` — Instructor, Registrar, DeptAdmin, Auditor can read invoice details. Restrict to `FinancePolicy` with student-ownership check (student can read own invoices). | High | Phase 2 |
| phase4-fix-8 | rbac-backend | `EduLearn.API/Controllers/TranscriptsController.cs:163,189` | `GET /api/transcripts/student/{studentId}` read endpoints use bare `[Authorize]` — Finance, DeptAdmin, Instructor, Auditor can read GPA and transcript data. Restrict to a `TranscriptViewPolicy` (Registrar, ITAdmin) plus student-ownership. | Medium | Phase 2 |
| phase4-fix-9 | rbac-backend | `EduLearn.API/Controllers/SectionsController.cs:122,147` | `GET /api/sections/{id}` and `GET /api/sections/course/{courseId}/term/{term}` use bare `[Authorize]` — Student, Finance, Auditor can fetch section details if they know an ID. Restrict to `RosterViewPolicy` (Instructor, Registrar, DeptAdmin, ITAdmin) or add explicit role list. | Medium | Phase 2 |
| phase4-fix-10 | rbac-backend | `EduLearn.API/Controllers/SubmissionsController.cs:223` | `GET /api/submissions/student/{studentId}` uses bare `[Authorize]` with student-ownership check only — DeptAdmin, Finance, Auditor pass through without scope restriction. Restrict to a policy covering Instructor, Registrar, ITAdmin with student-ownership for Student role. | Medium | Phase 2 |
| phase4-fix-11 | form-validation | `edulearn.client/src/pages/students/StudentDetailPage.jsx:170-179` | contactInfoJSON edit field is a raw textarea — no JSON validation, no field-level email/phone validation, no length limits. Malformed JSON is sent directly to the API. Replace with individual Email and Phone fields; build JSON in the submit handler; validate phone (10 digits) and email format. | Medium | Phase 3 |
| phase4-fix-12 | form-validation | `edulearn.client/src/pages/users/UsersPage.jsx:644-653` | Edit User modal Phone field has `maxLength={20}` only — no 10-digit enforcement, no inline feedback, no pre-submit guard in `handleEdit()`. Inconsistent with the Create modal. Add phone validation matching UsersPage.jsx:462-478. | Medium | Phase 3 |
| phase4-fix-13 | network-resilience | `edulearn.client/src/api/axiosClient.js:4` | No `timeout` set on the axios instance. Requests hang indefinitely when the backend is unreachable. Add `timeout: 15000` and handle `ECONNABORTED` in the response interceptor with a user-visible "Request timed out" message. | Medium | Phase 3 |
| phase4-fix-14 | error-handling | `edulearn.client/src/api/axiosClient.js:31-46` | No global 5xx handler. Dashboard components (InstructorDashboard, etc.) silently swallow API errors. Add a global interceptor branch for `status >= 500` that either dispatches a toast notification or logs visibly, so users know when a server error occurred. | Medium | Phase 3 |
| phase4-fix-15 | ux-safety | `edulearn.client/src/pages/students/NewStudentPage.jsx`, `CourseFormPage.jsx`, `ProgramFormPage.jsx`, `AssessmentFormPage.jsx` | No "unsaved changes" warning on navigate-away for full-page create/edit forms. Implement `useBlocker` (React Router v6) to prompt users before discarding entered data. | Medium | Phase 3 |
| phase4-fix-16 | pii-url | `edulearn.client/src/pages/students/NewStudentPage.jsx:18-20` | Applicant DOB (PII) is passed as a URL query parameter (`?dob=YYYY-MM-DD`), making it visible in browser history, server access logs, and referrer headers. Pass only `applicantID`; resolve the DOB server-side. | Low | Phase 3 |
| phase4-fix-17 | rbac-ux | `edulearn.client/src/components/Layout/Sidebar.jsx:65` | DeptAdmin sidebar shows a Timetable link but `EnrollmentViewPolicy` excludes DeptAdmin — navigating to `/timetable` returns a 403 from the backend. Remove the Timetable nav item for DeptAdmin role or extend `EnrollmentViewPolicy` to include DeptAdmin. | Medium | Phase 2 |
