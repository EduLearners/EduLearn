# EduLearn — Live Browser + Static Audit (2026-06-01)

Orchestrated multi-agent audit per `EduLearn_Audit_Prompt.md`. Live testing
drives the running app via Chrome DevTools MCP (visible browser) against
`http://localhost:5173` (API `https://localhost:5001`). Findings are appended
per agent below.

## Setup / environment

- **Servers:** frontend `:5173` (Vite), backend `:5001` https / `:5000` http — both confirmed up.
- **Working copy under test:** `…/Desktop/Vikash/EduLearn`, branch `Transh_fixing`.
- **Seed accounts** (all MFA-disabled → direct login, no TOTP needed for role journeys):

  | Username | Password | Role |
  |---|---|---|
  | admin | Admin@123 | ITAdmin |
  | student | Student@123 | Student |
  | instructor | Instructor@123 | Instructor |
  | registrar | Registrar@123 | Registrar |
  | deptadmin | DeptAdmin@123 | DeptAdmin |
  | finance | Finance@123 | Finance |
  | auditor | Auditor@123 | Auditor |

  Plus pre-existing rich data (Student1/studentID 1, courses, sections, invoices, tickets).

- **MFA strategy (audit §2):** test accounts created with MFA disabled (approach b/c).
  The MFA flow itself (enroll / wrong code / expired pending token / replay) is audited
  separately by Agent 1.
- **Known-intentional (NOT flagged):** JWT/config secrets in appsettings; `404 SECTIONS_NOT_FOUND`;
  `403` on policy-protected endpoints for low-priv roles; `401` on bad login; localStorage sessions.

## Pre-audit fixes applied & verified live (this session)

- **FIX-1 — Test Notification modal footer clipped** (commit `0a149e7`). Root cause: modal
  rendered inside the page-root div whose animation `transform` became the containing block for
  `position:fixed`, so the dialog sized against that div, not the viewport, and the Send/Cancel
  footer fell off short viewports. Fixed via `ModalPortal` + viewport-capped flex `.modal-content`
  with body-only scroll. Verified at 1280×600: Send button visible.
- **FIX-2 — New ticket did not notify ITAdmins** (commit `3a073aa`). `POST /api/tickets` audit-logged
  but notified no one. Now fans out an IT notification to every ITAdmin on creation. Verified live:
  student raised ticket #4 → admin bell went 0→1 with message "New ticket #4 … needs triage."
- **Verified + committed Vikash's 10 WIP fixes** (commit `af0ffda`): error-message hygiene
  (`getFriendlySimpleMessage`, no leaked status codes), Toast component, `step=any` rounding fix,
  stale-modal-error clearing, PublicOnlyRoute logout-on-/login, Sections client-filter, stale-transcript
  badge. Removed one dead `useEffect` import in ProtectedRoute.

---

# Findings

Severity: Critical / High / Medium / Low. Category: Security / Data-correctness /
Error-handling / Performance / UI-UX / Functional-bug.

## Data setup (completed) — full linked dataset for all 7 roles

Fixed `tests/seed-sample-data.ps1` (commit `d370b58`) and ran `-Reset`. Two seed bugs fixed:
case-insensitive `$U`/`$u` collision; `-Reset` left Courses populated (FK order) → now wipes with FK
constraints disabled; course IDs resolved by code. Result — every module populated and linked to the
named accounts: 2 programs, 6 courses (prereq chain), 3 rooms, 4 students, 5 sections, enrollments
(student→CS101+CS210, student2→CS101, student4→EE101, one drop), 4 assessments (3 published, 1 draft),
3 content items, 2 student submissions (1 graded=42), 2 applicants (1 accepted), 2 fees, 1 scholarship,
3 invoices, 2 payments, 2 notifications, 1 ticket (assigned+resolved), 2 transcripts (1 published).

Notes: enrollment in CS201 before CS101 is *completed* correctly returns 422 PREREQUISITES_NOT_MET
(prereq logic works). `student2` pre-exists with a non-default password (login 401 in seed) — its single
submission was skipped; not a product bug. `student` is fully data-rich and used for the Student journey.

## Agent 1 — Auth / IAM / Cross-cutting Security

### A1-01 — Student-role user with no linked Student record hangs the Enrollment page
- **Severity:** Medium · **Category:** Error-handling / Performance
- **Where:** `/enrollment` (frontend EnrollmentPage); `GET /api/students/me` → 404
- **Repro:** Log in as a Student-role user that has no linked Student domain record (e.g. a freshly
  created `student` user). Open `/enrollment`.
- **Expected:** A friendly message ("we couldn't find your student profile, contact the registrar")
  and the page settles.
- **Actual:** Page is stuck on "Resolving your student record..." indefinitely with dropdowns disabled,
  and `GET /api/students/me` is retried repeatedly (observed 4 identical 404 calls) — a failing-call loop.
- **Graceful verdict:** (1) data correct: Y — (2) friendly message: N (infinite spinner, no guidance) — (3) UI usable: N (stuck).
- **Suggested fix:** On 404 from `/api/students/me`, stop retrying and render an explanatory empty state.
- **Update:** Confirmed this only affects a Student-role user with NO linked Student record. A properly
  linked student (`student`/studentID 11) loads `/enrollment` fine (200, shows enrolled CS101+CS210). So
  the defect is the unhandled 404 + retry loop, not enrollment itself.

### A1-02 — CRITICAL: any Instructor can grade ANY submission (broken object-level authz / mutation IDOR)
- **Severity:** Critical · **Category:** Security
- **Where:** `POST /api/submissions/{id}/grade` — `EduLearn.API/Controllers/SubmissionsController.cs` (~line 150-216). The handler checks only `grader.Role == Instructor || ITAdmin`; it never verifies the caller owns the section the submission belongs to.
- **Repro:** Token for `instructor` (userID 31, owns **zero** sections). `POST /api/submissions/1/grade {"score":7}` where submission 1 belongs to a section taught by graderID 7. Result: **HTTP 200**, score changed **85 → 7**. (Restored to 85 after test.)
- **Expected vs Actual:** Expected 403 (not your section). Actual 200, grade overwritten; a GradeChange audit row is written under the wrong instructor.
- **Graceful verdict:** (1) data correct: **N — grade corrupted by unauthorized user** — (2) friendly msg: n/a — (3) UI usable: n/a.
- **Evidence:** `score BEFORE 85.0` → `POST .../grade -> 200` → `score AFTER 7.0`.
- **Suggested fix:** In the grade handler, load the submission→assessment→section and require `section.InstructorFK == callerId` (allow ITAdmin override). Apply the same ownership check to re-grade.

### A1-03 — HIGH: any Instructor can read ANY submission (object-level read IDOR)
- **Severity:** High · **Category:** Security
- **Where:** `GET /api/submissions/{id}` — SubmissionsController.
- **Repro:** `instructor` (userID 31, no sections) → `GET /api/submissions/1` → **200** with full data (studentName "Student1", score, `fileURI`, graderName). Submission belongs to another instructor's section.
- **Expected vs Actual:** Expected 403/404 for a non-owning instructor. Actual full disclosure of another instructor's student's submission + file link.
- **Graceful verdict:** (1) data correct: Y — (2) friendly: n/a — (3) usable: n/a — but **confidential data leaked**.
- **Suggested fix:** Same section-ownership check on read; students may read only their own, instructors only their sections', ITAdmin all.

### A1-04 — Login/validation hardening: PASS (no defect)
- Empty submit → native + custom "cannot be blank". SQL `' OR '1'='1` and bad password → clean **"Invalid username or password"** (no SQL exec, no 500, no leaked status code/route). Protected deep-link `/users` while logged out → redirected to `/login`. Recorded as PASS for evidence.

### Agent 1 — IDOR / authorization matrix (API)
| Role | Endpoint | Result | Verdict |
|---|---|---|---|
| Student | GET /api/users | 403 FORBIDDEN | ✅ correct |
| Student | GET /api/students/1 (not own) | 403 STUDENT_FORBIDDEN | ✅ correct |
| Student | GET /api/audit-log | 403 | ✅ correct |
| Student | GET /api/kpis | 403 | ✅ correct |
| Student | GET /api/submissions/1 | 403 | ✅ correct |
| Student | POST /api/sections | 403 | ✅ correct |
| Auditor | POST /api/sections | 403 | ✅ read-only enforced |
| Auditor | POST /api/fees | 403 | ✅ |
| Auditor | DELETE /api/invoices/1 | 405 (no route) | ✅ |
| Auditor | GET /api/audit-log | 200 | ✅ correct (read) |
| Instructor(no sections) | GET /api/users | 403 | ✅ |
| Instructor(no sections) | GET /api/submissions/1 | **200** | ❌ **A1-03 IDOR** |
| Instructor(no sections) | POST /api/submissions/1/grade | **200, grade changed** | ❌ **A1-02 CRITICAL** |

**Note (minor):** 403 JSON bodies include a `statusCode` field; the frontend's `getFriendlyError` ignores it, so not user-facing — not flagged.

**Note on A1-03 scope:** the Submissions *list* page is correctly scoped — as `instructor` (no sections) `/submissions` shows "My Sections → No submissions found." The leak is **only** via the direct `/submissions/{id}` route + API, confirmed visibly in the browser (the `/submissions/1/grade` page fully loaded another instructor's student submission with an editable score + Save Grade button — screenshot captured).

### A1-05 — MFA not enforced for privileged roles
- **Severity:** Medium · **Category:** Security
- **Where:** /users (admin view); all 36 users show MFA = "Off", including every Registrar, Finance, ITAdmin, Auditor, DeptAdmin.
- **Detail:** The audit brief states the five privileged roles are meant to be TOTP-MFA-challenged at login. In the running system MFA is disabled for all of them and they log in with username/password only. Either MFA enforcement for privileged roles is not implemented/active, or it is opt-in and no privileged account has enrolled.
- **Graceful verdict:** n/a (design/security gap, not a crash).
- **Suggested fix:** Enforce MFA enrollment for privileged roles (block privileged actions until enrolled), or document that MFA is opt-in for this scope.

### A1-06 — User list has no pagination
- **Severity:** Low · **Category:** UI-UX / Performance
- **Where:** /users — renders all 36 users in a single ungrouped table ("Showing 36 of 36"). No page size cap.
- **Detail:** Fine at 36 rows; will degrade (DOM size, scroll) as the user base grows. Other list pages should be checked for the same.
- **Suggested fix:** Add server-side pagination / a page-size cap to the users table (and audit other large lists).
